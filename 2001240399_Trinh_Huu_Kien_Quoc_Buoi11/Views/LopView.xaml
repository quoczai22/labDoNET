<UserControl x:Class="Lab11_ValidationNavigation.Views.LopView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Quan ly Lop" FontSize="26" FontWeight="Bold" Margin="0,0,0,18"/>

        <Grid Grid.Row="1" Margin="0,0,0,18">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="230"/>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="230"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Ma lop" VerticalAlignment="Center"/>
            <TextBox x:Name="txt_malop" Grid.Column="1" Text="{Binding MaLop, UpdateSourceTrigger=PropertyChanged}">
                <TextBox.Style>
                    <Style TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
                        <Style.Triggers>
                            <Trigger Property="Text" Value="">
                                <Setter Property="Background" Value="AliceBlue"/>
                                <Setter Property="ToolTip" Value="Ma lop khong trong"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </TextBox.Style>
            </TextBox>

            <TextBlock Grid.Column="2" Text="Ten lop" VerticalAlignment="Center"/>
            <TextBox Grid.Column="3" Text="{Binding TenLop, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Grid.Row="1" Text="Khoa" VerticalAlignment="Center"/>
            <ComboBox Grid.Row="1" Grid.Column="1" Height="30" Margin="0,4,14,10"
                      ItemsSource="{Binding DS_Khoa}" DisplayMemberPath="TenKhoa"
                      SelectedValuePath="MaKhoa" SelectedValue="{Binding MaKhoa}"/>

            <StackPanel Grid.Row="1" Grid.Column="2" Grid.ColumnSpan="3" Orientation="Horizontal" Margin="0,4,0,0">
                <Button Content="Them" Command="{Binding AddCommand}"/>
                <Button Content="Sua" Command="{Binding EditCommand}"/>
                <Button Content="Luu" Command="{Binding SaveCommand}"/>
                <Button Content="Huy" Command="{Binding CancelCommand}"/>
                <Button Content="Xoa" Command="{Binding DeleteCommand}"/>
            </StackPanel>
        </Grid>

        <Grid Grid.Row="2">
            <DataGrid x:Name="dgLop" ItemsSource="{Binding DS_Lop}" SelectedItem="{Binding SelectedLop}"
                      AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Ma lop" Binding="{Binding MaLop}" Width="150"/>
                    <DataGridTextColumn Header="Ten lop" Binding="{Binding TenLop}" Width="*"/>
                    <DataGridTextColumn Header="Ma khoa" Binding="{Binding MaKhoa}" Width="150"/>
                </DataGrid.Columns>
            </DataGrid>

            <TextBlock Text="Chua co du lieu lop" Foreground="Gray" FontStyle="Italic"
                       HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ElementName=dgLop, Path=Items.Count}" Value="0">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
        </Grid>
    </Grid>
</UserControl>
