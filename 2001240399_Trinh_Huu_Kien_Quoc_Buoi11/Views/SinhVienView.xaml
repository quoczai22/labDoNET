<UserControl x:Class="Lab11_ValidationNavigation.Views.SinhVienView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Quan ly Sinh vien" FontSize="26" FontWeight="Bold" Margin="0,0,0,18"/>

        <Grid Grid.Row="1" Margin="0,0,0,18">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="230"/>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="230"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Ma SV" VerticalAlignment="Center"/>
            <TextBox Grid.Column="1" Text="{Binding NewSinhVien.MaSV, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"/>
            <TextBlock Grid.Column="2" Text="Ho ten" VerticalAlignment="Center"/>
            <TextBox Grid.Column="3" Text="{Binding NewSinhVien.HoTen, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"/>
            <TextBlock Grid.Row="1" Text="Lop" VerticalAlignment="Center"/>
            <ComboBox Grid.Row="1" Grid.Column="1" Height="30" Margin="0,4,14,10"
                      ItemsSource="{Binding DS_Lop}" DisplayMemberPath="TenLop"
                      SelectedValuePath="MaLop" SelectedValue="{Binding NewSinhVien.MaLop}"/>
            <TextBlock Grid.Row="1" Grid.Column="2" Text="Tuoi" VerticalAlignment="Center"/>
            <TextBox Grid.Row="1" Grid.Column="3" Text="{Binding NewSinhVien.Tuoi, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"/>

            <StackPanel Grid.Row="2" Grid.ColumnSpan="5" Orientation="Horizontal" Margin="120,4,0,0">
                <Button Content="Them" Command="{Binding AddCommand}"/>
                <Button Content="Cap nhat" Command="{Binding UpdateCommand}"/>
                <Button Content="Xoa" Command="{Binding DeleteCommand}"/>
                <Button Content="Nhap lai" Command="{Binding ClearCommand}"/>
            </StackPanel>
        </Grid>

        <DataGrid Grid.Row="2" ItemsSource="{Binding DS_SinhVien}" SelectedItem="{Binding SelectedSinhVien}"
                  AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Ma SV" Binding="{Binding MaSV}" Width="130"/>
                <DataGridTextColumn Header="Ho ten" Binding="{Binding HoTen}" Width="*"/>
                <DataGridTextColumn Header="Ma lop" Binding="{Binding MaLop}" Width="150"/>
                <DataGridTextColumn Header="Tuoi" Binding="{Binding Tuoi}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
