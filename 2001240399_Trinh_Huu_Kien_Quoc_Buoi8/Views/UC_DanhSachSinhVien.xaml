<UserControl x:Class="_2001240399_Trinh_Huu_Kien_Quoc_Buoi8.Views.UC_DanhSachSinhVien"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:_2001240399_Trinh_Huu_Kien_Quoc_Buoi8.Views"
             mc:Ignorable="d" 
             Background="White"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="Xuất file" Width="80" Margin="0,0,20,0" Command="{Binding XuatFileCommand}"/>
            <Label Content="Tìm theo Tên:"/>
            <TextBox Width="200" Margin="0,0,20,0" Text="{Binding TuKhoaTimKiem, UpdateSourceTrigger=PropertyChanged}"/>
            <Label Content="Tìm theo Lớp:"/>
            <ComboBox Width="100" ItemsSource="{Binding DanhSachLopLoc}" DisplayMemberPath="TenLop" SelectedItem="{Binding LopLocDangChon}"/>
        </StackPanel>

        <DataGrid Grid.Row="1" ItemsSource="{Binding SinhVienView}" SelectedItem="{Binding SelectedStudentGrid}" AutoGenerateColumns="False" GridLinesVisibility="Horizontal">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Mã SV" Binding="{Binding MaSV}" Width="*"/>
                <DataGridTextColumn Header="Họ tên" Binding="{Binding HoTen}" Width="2*"/>
                <DataGridTextColumn Header="Lớp" Binding="{Binding TenLop}" Width="*"/>
                <DataGridTextColumn Header="Giới tính" Binding="{Binding GioiTinhText}" Width="*"/>
                <DataGridTextColumn Header="Thành phố" Binding="{Binding ThanhPho}" Width="*"/>
                <DataGridTextColumn Header="Địa chỉ" Binding="{Binding DiaChi}" Width="2*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
