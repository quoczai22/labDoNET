<Window x:Class="Lab07_Bai1_Cafe.Views.CafeView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Lab07_Bai1_Cafe.Views"
        xmlns:vm="clr-namespace:Lab07_Bai1_Cafe.ViewModels"
        mc:Ignorable="d"
        Title="CafeView" Height="650" Width="1100">
    <Window.DataContext>
        <vm:CafeViewModel/>
    </Window.DataContext>

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" BorderBrush="Gray" BorderThickness="1" Padding="10" Margin="0,0,10,0">
            <StackPanel>
                <TextBlock Text="THÔNG TIN GỌI MÓN" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>

                <TextBlock Text="Tên khách hàng"/>
                <TextBox Text="{Binding CustomerName, UpdateSourceTrigger=PropertyChanged}" Height="28" Margin="0,5,0,10"/>

                <TextBlock Text="Số điện thoại"/>
                <TextBox Text="{Binding Phone, UpdateSourceTrigger=PropertyChanged}" Height="28" Margin="0,5,0,10"/>

                <CheckBox Content="Là sinh viên" IsChecked="{Binding IsStudent}" Margin="0,0,0,10"/>

                <TextBlock Text="Chọn bàn" FontWeight="Bold"/>
                <ComboBox ItemsSource="{Binding Tables}" SelectedItem="{Binding SelectedTable}" Height="28" Margin="0,5,0,10"/>

                <TextBlock Text="Chọn thức ăn" FontWeight="Bold" Margin="0,5,0,5"/>
                <CheckBox Content="Bánh mì trứng" IsChecked="{Binding Food1}"/>
                <CheckBox Content="Bánh mì cá" IsChecked="{Binding Food2}"/>
                <CheckBox Content="Mì tôm trứng" IsChecked="{Binding Food3}"/>
                <CheckBox Content="Mì xào bò" IsChecked="{Binding Food4}"/>
                <CheckBox Content="Mì cay" IsChecked="{Binding Food5}"/>

                <TextBlock Text="Chọn nước uống" FontWeight="Bold" Margin="0,10,0,5"/>
                <CheckBox Content="Cafe đen" IsChecked="{Binding Drink1}"/>
                <CheckBox Content="Cafe đá" IsChecked="{Binding Drink2}"/>
                <CheckBox Content="Cafe sữa" IsChecked="{Binding Drink3}"/>
                <CheckBox Content="Cafe sữa đá" IsChecked="{Binding Drink4}"/>
                <CheckBox Content="Cafe kem" IsChecked="{Binding Drink5}"/>

                <WrapPanel Margin="0,15,0,0">
                    <Button Content="Chọn" Width="90" Height="32" Margin="5" Command="{Binding CreateCommand}"/>
                    <Button Content="Thanh toán" Width="100" Height="32" Margin="5" Command="{Binding CheckoutCommand}"/>
                    <Button Content="Nhập lại" Width="90" Height="32" Margin="5" Command="{Binding ResetCommand}"/>
                </WrapPanel>
            </StackPanel>
        </Border>

        <Border Grid.Column="1" BorderBrush="Gray" BorderThickness="1" Padding="10">
            <DockPanel>
                <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" Margin="0,10,0,0">
                    <Button Content="Xóa hóa đơn" Width="110" Height="30" Margin="0,0,20,0" Command="{Binding DeleteCommand}"/>
                    <TextBlock Text="Tổng khách hàng: " FontWeight="Bold" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding TotalCustomerCount}" Margin="0,0,20,0" VerticalAlignment="Center"/>
                    <TextBlock Text="Tổng tiền: " FontWeight="Bold" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding TotalRevenue, StringFormat=N0}" VerticalAlignment="Center"/>
                </StackPanel>

                <DataGrid ItemsSource="{Binding InvoicesView}"
                          SelectedItem="{Binding SelectedInvoice}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Tên KH" Binding="{Binding CustomerName}" Width="120"/>
                        <DataGridTextColumn Header="SĐT" Binding="{Binding Phone}" Width="100"/>
                        <DataGridTextColumn Header="Bàn" Binding="{Binding TableName}" Width="70"/>
                        <DataGridTextColumn Header="Sinh viên" Binding="{Binding IsStudent}" Width="80"/>
                        <DataGridTextColumn Header="Thức ăn" Binding="{Binding FoodText}" Width="180"/>
                        <DataGridTextColumn Header="Nước uống" Binding="{Binding DrinkText}" Width="180"/>
                        <DataGridTextColumn Header="Tạm tính" Binding="{Binding SubTotal, StringFormat=N0}" Width="100"/>
                        <DataGridTextColumn Header="Giảm giá" Binding="{Binding Discount, StringFormat=N0}" Width="100"/>
                        <DataGridTextColumn Header="Tổng tiền" Binding="{Binding Total, StringFormat=N0}" Width="100"/>
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </Border>
    </Grid>
</Window>
