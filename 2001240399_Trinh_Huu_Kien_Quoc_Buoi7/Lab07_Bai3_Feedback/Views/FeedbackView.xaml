<Window x:Class="Lab07_Bai3_Feedback.Views.FeedbackView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Lab07_Bai3_Feedback.Views"
        xmlns:vm="clr-namespace:Lab07_Bai3_Feedback.ViewModels"
        mc:Ignorable="d"
        Title="FeedbackView" Height="450" Width="800">

    <Window.DataContext>
        <vm:FeedbackViewModel/>
    </Window.DataContext>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <GroupBox Header="Thông tin khách hàng" Margin="0,0,0,10">
            <Grid Margin="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="Tên khách hàng" VerticalAlignment="Center" Margin="5"/>
                <TextBox Grid.Column="1" Text="{Binding CustomerName, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>

                <TextBlock Grid.Column="2" Text="Số điện thoại" VerticalAlignment="Center" Margin="5"/>
                <TextBox Grid.Column="3" Text="{Binding Phone, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>
            </Grid>
        </GroupBox>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>

            <GroupBox Header="Góp ý" Grid.Column="0" Margin="0,0,10,0">
                <DockPanel Margin="10">
                    <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" Margin="0,10,0,0">
                        <Button Content="Gửi góp ý" Width="100" Margin="5" Command="{Binding SendSuggestionCommand}"/>
                    </StackPanel>

                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="140"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <TextBox Grid.Row="0"
                                 Text="{Binding SuggestionText, UpdateSourceTrigger=PropertyChanged}"
                                 AcceptsReturn="True"
                                 VerticalScrollBarVisibility="Auto"/>

                        <DataGrid Grid.Row="1"
                                  Margin="0,10,0,0"
                                  ItemsSource="{Binding Suggestions}"
                                  AutoGenerateColumns="False"
                                  CanUserAddRows="False"
                                  IsReadOnly="True">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Tên KH" Binding="{Binding CustomerName}" Width="100"/>
                                <DataGridTextColumn Header="SĐT" Binding="{Binding Phone}" Width="100"/>
                                <DataGridTextColumn Header="Nội dung góp ý" Binding="{Binding Content}" Width="*"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </DockPanel>
            </GroupBox>

            <GroupBox Header="Phản hồi đánh giá" Grid.Column="1">
                <DockPanel Margin="10">
                    <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" Margin="0,10,0,0">
                        <Button Content="Lưu phản hồi" Width="100" Margin="5" Command="{Binding SaveResponseCommand}"/>
                        <Button Content="Nhập lại" Width="100" Margin="5" Command="{Binding ResetCommand}"/>
                    </StackPanel>

                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="0" Margin="0,0,0,10">
                            <TextBlock Text="Bạn đánh giá chất lượng dịch vụ như thế nào?" FontWeight="Bold"/>
                            <ComboBox ItemsSource="{Binding Answers}" SelectedItem="{Binding SelectedServiceAnswer}" Margin="0,5,0,0"/>
                        </StackPanel>

                        <StackPanel Grid.Row="1" Margin="0,0,0,10">
                            <TextBlock Text="Bạn đánh giá chất lượng sản phẩm như thế nào?" FontWeight="Bold"/>
                            <ComboBox ItemsSource="{Binding Answers}" SelectedItem="{Binding SelectedProductAnswer}" Margin="0,5,0,0"/>
                        </StackPanel>

                        <StackPanel Grid.Row="2" Margin="0,0,0,10">
                            <TextBlock Text="Bạn đánh giá thái độ phục vụ như thế nào?" FontWeight="Bold"/>
                            <ComboBox ItemsSource="{Binding Answers}" SelectedItem="{Binding SelectedAttitudeAnswer}" Margin="0,5,0,0"/>
                        </StackPanel>

                        <DataGrid Grid.Row="3"
                                  ItemsSource="{Binding Responses}"
                                  AutoGenerateColumns="False"
                                  CanUserAddRows="False"
                                  IsReadOnly="True">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Tên KH" Binding="{Binding CustomerName}" Width="100"/>
                                <DataGridTextColumn Header="SĐT" Binding="{Binding Phone}" Width="100"/>
                                <DataGridTextColumn Header="Dịch vụ" Binding="{Binding ServiceAnswer}" Width="120"/>
                                <DataGridTextColumn Header="Sản phẩm" Binding="{Binding ProductAnswer}" Width="120"/>
                                <DataGridTextColumn Header="Phục vụ" Binding="{Binding AttitudeAnswer}" Width="120"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </DockPanel>
            </GroupBox>
        </Grid>
    </Grid>
</Window>
