<UserControl x:Class="Lab11_ValidationNavigation.Views.KhoaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Quan ly Khoa" FontSize="26" FontWeight="Bold" Margin="0,0,0,18"/>

        <Grid Grid.Row="1" Margin="0,0,0,18">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="130"/>
                <ColumnDefinition Width="260"/>
                <ColumnDefinition Width="130"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Ma khoa" VerticalAlignment="Center"/>
            <TextBox Grid.Column="1" Text="{Binding NewKhoa.MaKhoa, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True, NotifyOnValidationError=True}"/>
            <TextBlock Grid.Column="2" Text="Ten khoa" VerticalAlignment="Center"/>
            <TextBox Grid.Column="3" Text="{Binding NewKhoa.TenKhoa, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True, NotifyOnValidationError=True}"/>

            <StackPanel Grid.Row="1" Grid.ColumnSpan="4" Orientation="Horizontal" Margin="130,6,0,0">
                <Button Content="Them" Command="{Binding AddCommand}"/>
                <Button Content="Sua" Command="{Binding UpdateCommand}"/>
                <Button Content="Xoa" Command="{Binding DeleteCommand}"/>
                <Button Content="Nhap lai" Command="{Binding ClearCommand}"/>
            </StackPanel>
        </Grid>

        <DataGrid Grid.Row="2" ItemsSource="{Binding DS_Khoa}" SelectedItem="{Binding SelectedKhoa}"
                  AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Ma khoa" Binding="{Binding MaKhoa}" Width="160"/>
                <DataGridTextColumn Header="Ten khoa" Binding="{Binding TenKhoa}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
